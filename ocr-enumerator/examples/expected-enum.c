/* Intended output after reviewing/correcting the synthetic table.
 * This is a reference, not a recorded OCR result. */
typedef enum {
    STATE_IDLE = 0,
    STATE_READY = 1,
    STATE_RUNNING = 2,
} MyEnum;
